name: Test CI

on:
  workflow_dispatch

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Find version number
      run: export SOME_VERSION=$(python --version)
      
    - name: Push to dockerhub
      run: echo ${{ env.SOME_VERSION }}
    
